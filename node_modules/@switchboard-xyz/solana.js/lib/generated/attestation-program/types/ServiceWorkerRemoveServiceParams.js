import * as borsh from "@coral-xyz/borsh";
export class ServiceWorkerRemoveServiceParams {
    idx;
    constructor(fields) {
        this.idx = fields.idx;
    }
    static layout(property) {
        return borsh.struct([borsh.option(borsh.u32(), "idx")], property);
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    static fromDecoded(obj) {
        return new ServiceWorkerRemoveServiceParams({
            idx: obj.idx,
        });
    }
    static toEncodable(fields) {
        return {
            idx: fields.idx,
        };
    }
    toJSON() {
        return {
            idx: this.idx,
        };
    }
    static fromJSON(obj) {
        return new ServiceWorkerRemoveServiceParams({
            idx: obj.idx,
        });
    }
    toEncodable() {
        return ServiceWorkerRemoveServiceParams.toEncodable(this);
    }
}
