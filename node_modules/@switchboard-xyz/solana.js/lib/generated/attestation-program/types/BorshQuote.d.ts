import * as types from "../types/index.js";
import { PublicKey } from "@solana/web3.js";
import { BN } from "@switchboard-xyz/common";
export interface BorshQuoteFields {
    /** The address of the signer generated within an enclave. */
    enclaveSigner: PublicKey;
    /** The [`VerifierAccountData`] which completed the verification. */
    verifier: PublicKey;
    /** The quotes MRENCLAVE measurement dictating the contents of the secure enclave. */
    mrEnclave: Array<number>;
    /** The off-chain registry where the verifiers quote can be located. */
    quoteRegistry: Array<number>;
    /** Key to lookup the buffer data on IPFS or an alternative decentralized storage solution. */
    registryKey: Array<number>;
    verificationStatus: number;
    /** The unix timestamp when verification was requested. */
    requestTimestamp: BN;
    /** The unix timestamp when the quote was last verified. */
    verificationTimestamp: BN;
    /** The unix timestamp when the quotes verification status expires. */
    validUntil: BN;
    ebuf: Array<number>;
}
export interface BorshQuoteJSON {
    /** The address of the signer generated within an enclave. */
    enclaveSigner: string;
    /** The [`VerifierAccountData`] which completed the verification. */
    verifier: string;
    /** The quotes MRENCLAVE measurement dictating the contents of the secure enclave. */
    mrEnclave: Array<number>;
    /** The off-chain registry where the verifiers quote can be located. */
    quoteRegistry: Array<number>;
    /** Key to lookup the buffer data on IPFS or an alternative decentralized storage solution. */
    registryKey: Array<number>;
    verificationStatus: number;
    /** The unix timestamp when verification was requested. */
    requestTimestamp: string;
    /** The unix timestamp when the quote was last verified. */
    verificationTimestamp: string;
    /** The unix timestamp when the quotes verification status expires. */
    validUntil: string;
    ebuf: Array<number>;
}
export declare class BorshQuote {
    /** The address of the signer generated within an enclave. */
    readonly enclaveSigner: PublicKey;
    /** The [`VerifierAccountData`] which completed the verification. */
    readonly verifier: PublicKey;
    /** The quotes MRENCLAVE measurement dictating the contents of the secure enclave. */
    readonly mrEnclave: Array<number>;
    /** The off-chain registry where the verifiers quote can be located. */
    readonly quoteRegistry: Array<number>;
    /** Key to lookup the buffer data on IPFS or an alternative decentralized storage solution. */
    readonly registryKey: Array<number>;
    readonly verificationStatus: number;
    /** The unix timestamp when verification was requested. */
    readonly requestTimestamp: BN;
    /** The unix timestamp when the quote was last verified. */
    readonly verificationTimestamp: BN;
    /** The unix timestamp when the quotes verification status expires. */
    readonly validUntil: BN;
    readonly ebuf: Array<number>;
    constructor(fields: BorshQuoteFields);
    static layout(property?: string): any;
    static fromDecoded(obj: any): types.BorshQuote;
    static toEncodable(fields: BorshQuoteFields): {
        enclaveSigner: PublicKey;
        verifier: PublicKey;
        mrEnclave: number[];
        quoteRegistry: number[];
        registryKey: number[];
        verificationStatus: number;
        requestTimestamp: BN;
        verificationTimestamp: BN;
        validUntil: BN;
        ebuf: number[];
    };
    toJSON(): BorshQuoteJSON;
    static fromJSON(obj: BorshQuoteJSON): BorshQuote;
    toEncodable(): {
        enclaveSigner: PublicKey;
        verifier: PublicKey;
        mrEnclave: number[];
        quoteRegistry: number[];
        registryKey: number[];
        verificationStatus: number;
        requestTimestamp: BN;
        verificationTimestamp: BN;
        validUntil: BN;
        ebuf: number[];
    };
}
//# sourceMappingURL=BorshQuote.d.ts.map