import * as types from "../types/index.js"; // eslint-disable-line @typescript-eslint/no-unused-vars
import * as borsh from "@coral-xyz/borsh"; // eslint-disable-line @typescript-eslint/no-unused-vars
import { TransactionInstruction } from "@solana/web3.js"; // eslint-disable-line @typescript-eslint/no-unused-vars
export const layout = borsh.struct([
    types.ServiceAddWorkerParams.layout("params"),
]);
export function functionServiceAddWorker(program, args, accounts, programId = program.attestationProgramId) {
    const keys = [
        { pubkey: accounts.serviceWorker, isSigner: false, isWritable: true },
        { pubkey: accounts.service, isSigner: false, isWritable: true },
        { pubkey: accounts.function, isSigner: false, isWritable: false },
        { pubkey: accounts.authority, isSigner: true, isWritable: false },
    ];
    const identifier = Buffer.from([123, 126, 200, 83, 231, 199, 42, 162]);
    const buffer = Buffer.alloc(1000);
    const len = layout.encode({
        params: types.ServiceAddWorkerParams.toEncodable(args.params),
    }, buffer);
    const data = Buffer.concat([identifier, buffer]).slice(0, 8 + len);
    const ix = new TransactionInstruction({ keys, programId, data });
    return ix;
}
